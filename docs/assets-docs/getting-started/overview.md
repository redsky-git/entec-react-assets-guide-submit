---
sidebar_position: 1
displayed_sidebar: "assetsDocSidebar"
title: "개요"
---

# entec-react-assets 개요


## 소개
---

**entec-react-assets**는 React와 TypeScript 기반으로 구축된 재사용 가능한 UI 컴포넌트, 유틸리티 라이브러리 및 데모 플랫폼입니다.  <br /><br />
이 프로젝트는 개발자들이 실제로 동작하는 컴포넌트와 유틸리티를 미리 보고, 예제 코드를 복사하여 자신의 프로젝트에 즉시 적용할 수 있도록 설계되었습니다.

### 주요 목적

- **컴포넌트 라이브러리 제공**: shadcn/ui 기반의 고품질 UI 컴포넌트를 래핑(wrap)한 자체 컴포넌트들을 제공합니다.
- **유틸리티 라이브러리 제공**: 다양한 유틸리티 함수 및 헬퍼(예: API통신, 날짜 포맷, 배열/객체 조작, 데이터 변환 등)를 제공합니다.
- **인터랙티브 데모**: 각 컴포넌트 및 유틸리티의 실제 동작을 확인할 수 있는 라이브 데모 환경을 제공합니다.
- **코드 예제 제공**: 컴포넌트 및 유틸리티 사용법과 예제 코드를 바로 복사하여 사용할 수 있습니다.
- **개발 생산성 향상**: 반복적인 UI 컴포넌트 개발 시간을 단축하고, 일관된 디자인 시스템을 유지할 수 있습니다. 또한 자주 사용하는 유틸리티 함수들을 제공하여 API 통신, 데이터 포맷팅, 배열/객체 처리 등 다양한 비즈니스 로직 구현 시간을 줄여줍니다.

### 제공 UI 컴포넌트

현재 제공되는 주요 컴포넌트는 다음과 같습니다. (계속 추가 중)

- **Accordion**: 접이식 콘텐츠 패널
- **Alert & AlertDialog**: 알림 및 확인 다이얼로그
- **Badge**: 상태 표시 배지
- **Button**: 다양한 스타일의 버튼
- **Calendar**: 날짜 선택 캘린더
- **Checkbox**: 체크박스 입력
- **Dialog**: 모달 다이얼로그
- **Icon**: 아이콘 컴포넌트
- **Input**: 텍스트 입력 필드
- **Select**: 드롭다운 선택
- **Spinner**: 로딩 스피너
- **Table**: 데이터 테이블


### 제공 유틸리티

현재 제공되는 주요 유틸리티는 다음과 같습니다. (지속적으로 추가 중)

- **API Helper**: Axios 기반의 API 통신 함수 및 공통 request/error 핸들러
- **Date Utils**: 날짜 포맷 변환, 상대 날짜 계산 등 날짜 처리 함수
- **Array/Object Utils**: 배열, 객체 조작을 위한 다양한 헬퍼 함수 (정렬, 필터, 딥 클론 등)
- **Data Formatter**: 통화, 숫자, 퍼센트 등 각종 데이터 포맷 변환 함수
- **Validation Utils**: 이메일, 휴대폰 번호, 숫자 등 다양한 입력값 유효성 검사 함수
- **Storage Utils**: 로컬스토리지, 세션스토리지 편의 함수
- **String Utils**: 문자열 변환, 치환, 대소문자 변경 관련 함수
- **Clipboard Utils**: 텍스트, 객체 클립보드 복사 함수 및 지원 여부 체크








## 장점
---

### 개발 생산성 향상

- **즉시 사용 가능한 UI 컴포넌트**: 자주 사용하는 UI 컴포넌트를 제공하므로 import하여 바로 사용할 수 있어 개발 시간을 크게 단축할 수 있습니다. 또한, 프로젝트 상황에 맞게 스타일의 유연성과 커스터마이징 용이성 측면에서도 매우 큰 장점이 있습니다.
- **일관된 디자인 시스템**: 통일된 디자인 가이드라인을 따르는 컴포넌트로 일관성 있는 UI를 구축할 수 있습니다.
- **코드 예제 제공**: 각 컴포넌트마다 실제 사용 예제와 코드를 제공하여 학습 곡선을 낮추고, 개발 생산성을 향상시킵니다.

### 타입 안정성

- **TypeScript 기반**: 모든 컴포넌트가 TypeScript로 작성되어 타입 안정성을 보장합니다.
- **명확한 타입 정의**: 컴포넌트 props와 반환값에 대한 명확한 타입 정의로 개발 시 오류를 사전에 방지합니다.
- **IDE 자동완성 지원**: TypeScript의 강력한 타입 시스템으로 IDE(VSCode)에서 자동완성과 타입 체크를 제공합니다.

### 접근성 (Accessibility)

- **Radix UI 기반**: 접근성 표준을 준수하는 Radix UI 프리미티브를 기반으로 구축되어 있습니다.
- **키보드 네비게이션**: 모든 컴포넌트가 키보드로 완전히 조작 가능합니다.
- **스크린 리더 지원**: ARIA 속성을 적절히 사용하여 스크린 리더 사용자를 지원합니다.

### 커스터마이징 용이성

- **Tailwind CSS 기반**: Tailwind CSS를 사용하여 스타일을 쉽게 커스터마이징할 수 있습니다.
- **UI 컴포넌트**: 기존에 널리 사용되는 UI 프레임워크(**MUI, AntD, Chakra UI**)의 컴포넌트가 아닌, **헤드리스 컴포넌트** 방식을 도입하여 프로젝트 상황에 맞게 자유롭게 수정하고 확장할 수 있습니다.
- **유연한 스타일링**: 상황에 따라 **Tailwind CSS**를 사용하지 않고 Sass, Emotion, CSS Modules 등 원하는 어떤 스타일링 기술을 사용하든 쉽게 적용 가능합니다.
  :::info 헤드리스 컴포넌트란?
  **헤드리스 컴포넌트**는 **사용자 인터페이스(UI) 없이** 컴포넌트의 핵심 **기능 로직**과 **상태 관리**만을 담당하는 디자인 패턴입니다. 말 그대로 '머리(Head, 즉 시각적인 UI)'가 없는 컴포넌트입니다.

  UI 구현은 외부에 위임하며, 개발자는 이 헤드리스 컴포넌트가 제공하는 로직과 상태를 활용하여 **자신만의 커스텀된 UI**를 자유롭게 만들 수 있습니다.
  :::

### 현대적인 아키텍처

- **도메인 기반 구조(DDD)**: 기능 또는 업무별로 도메인을 분리하여 소스코드의 복잡도와 충돌가능성을 낮추고, 유지보수성과 확장성을 크게 향상시켰습니다.
- **재사용 가능한 유틸리티**: 공통 API 모듈, 유틸리티 함수, 상태 관리 로직을 제공합니다.
- **모듈화된 설계**: 각 컴포넌트가 독립적으로 동작하며 필요한 부분만 선택적으로 사용할 수 있습니다.

### 상태 관리 통합

- **Redux Toolkit 통합**: 강력한 상태 관리 시스템을 내장하여 복잡한 애플리케이션 상태를 효율적으로 관리할 수 있습니다. 또한, 상태 관리 로직을 최대한 쉽게 사용할 수 있도록 관련 Hooks도 제공합니다.
- **API 통합 지원**: Axios 기반의 공통 API 클라이언트 및 Hooks를 제공하여 백엔드와의 통신을 간소화합니다.
- **중복 요청 방지**: API 호출 시 중복 요청을 자동으로 방지하는 메커니즘을 내장하고 있습니다.

### 개발자 경험 (DX)

- **인터랙티브 문서**: UI 컴포넌트를 직접 조작해보며 동작을 확인할 수 있는 라이브 데모를 제공합니다.
- **전역 유틸리티 객체**: `$router`, `$util`, `$ui` 등의 객체을 통해 편리한 전역 접근을 제공하고, 자주 사용하는 로직을 구현할 필요없이 바로 사용할 수 있습니다.








## 기반 기술

### 핵심 프레임워크 및 라이브러리

- **React 19.1.1**: 최신 버전의 React를 사용하여 최신 기능과 성능 최적화를 활용합니다.
- **TypeScript 5.9.3**: 정적 타입 검사를 통한 안정적인 코드 작성.
- **Vite 7.1.7**: 빠른 개발 서버와 최적화된 프로덕션 빌드를 제공하는 차세대 빌드 도구.


### UI 프레임워크 및 스타일링

- **Tailwind CSS 4.1.14**: 유틸리티 우선 CSS 프레임워크로 빠르고 일관된 스타일링
- **Radix UI**: 접근성을 고려한 헤드리스 UI 프리미티브 라이브러리
  - `@radix-ui/react-accordion`
  - `@radix-ui/react-alert-dialog`
  - `@radix-ui/react-dialog`
  - `@radix-ui/react-select`
  - 기타 Radix UI 컴포넌트들
- **Lucide React**: 현대적이고 일관된 아이콘 세트
- **class-variance-authority**: 컴포넌트 variant 관리를 위한 유틸리티

### 상태 관리 및 데이터 페칭

- **Redux Toolkit 2.9.2**: 효율적인 상태 관리와 액션 생성
- **React Redux 9.2.0**: React와 Redux 통합
- **Axios 1.13.0**: HTTP 클라이언트로 API 통신 처리
  - 인터셉터를 통한 요청/응답 처리
  - 중복 요청 방지 메커니즘
  - 에러 핸들링

### 라우팅

- **React Router 7.9.4**: 클라이언트 사이드 라우팅
- **@loadable/component**: 코드 스플리팅을 통한 성능 최적화

### 날짜 및 시간 처리

- **date-fns 4.1.0**: 날짜 유틸리티 라이브러리
- **dayjs 1.11.19**: 경량 날짜 라이브러리
- **react-day-picker 9.11.1**: React용 날짜 선택 컴포넌트

### 개발 도구

- **ESLint 9.36.0**: 코드 품질 및 일관성 검사
- **Prettier 3.6.2**: 코드 포맷팅
- **TypeScript ESLint**: TypeScript 전용 린팅 규칙
- **@vitejs/plugin-react-swc**: SWC를 사용한 빠른 React 컴파일


### 프로젝트 구조

**entec-react-assets** 프로젝트는 다음과 같은 구조로 구성되어 있습니다:

```
src/
├── app/                    # 애플리케이션 코어
│   ├── common/             # 공통 유틸리티 및 API 클라이언트
│   ├── components/         # 재사용 가능한 UI 컴포넌트
│   ├── hooks/              # 커스텀 React 훅
│   ├── router/             # 라우터 설정
│   ├── store/              # Redux 스토어 설정
│   └── types/              # TypeScript 타입 정의
├── assets/                 # 에셋(정적 파일: 이미지, 아이콘, 스타일 등)
├── domains/                # 도메인별 기능 모듈
│   ├── example/            # 컴포넌트 예제 및 데모
│   └── main/               # 메인 페이지
└── shared/                 # 공유 컴포넌트 및 상수
    ├── components/         # 공유 레이아웃 컴포넌트
    ├── constants/          # 상수 데이터
    ├── router/             # 공유 라우터 설정
    └── store/              # 공유 스토어 설정
```

<!-- 이러한 기술 스택과 구조를 통해 **entec-react-assets**는 현대적이고 확장 가능하며 유지보수가 용이한 React 애플리케이션 개발을 지원합니다. -->
