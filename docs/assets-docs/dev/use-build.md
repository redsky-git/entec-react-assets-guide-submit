---
sidebar_position: 4
displayed_sidebar: "assetsDocSidebar"
title: "빌드"
---

# 빌드

:::info 내용
- 개발이 완료된 후 실제 서비스에 배포하기 위해 소스 코드를 빌드해야 합니다.
- `npm run build` 명령어를 실행하면, 최적화된 정적 파일들이 `build` 폴더에 생성됩니다.
- 빌드 과정에서는 코드 압축, 환경 변수 적용, 번들링 등이 자동으로 처리됩니다.
- 빌드된 파일들은 웹 서버(예: Nginx, AWS S3 등)에 업로드하여 배포할 수 있습니다.
:::


## 빌드 진행 방법

1. **의존성 설치**  
   프로젝트에서는 `package.json`에 명시된 **의존성 패키지**가 모두 설치되어 있어야 합니다.  
   설치되어 있지 않다면, 프로젝트 루트 디렉터리에서 아래 명령어로 필요한 패키지를 먼저 설치합니다.
   ```bash
   npm install
   ```

2. **개발 모드에서 정상 동작 확인**
   빌드 전, 개발 서버를 실행하여 애플리케이션이 정상적으로 동작하는지 확인합니다.
   ```bash
   npm run dev
   ```

3. **환경 변수 설정**
   `.env.production` 등의 환경 변수 파일을 필요에 따라 설정합니다.<br />
   (예시: API_ENDPOINT, PUBLIC_URL 등)

4. **프로덕션 빌드 실행**
   최적화된 배포 파일을 생성하려면 아래 명령어를 실행합니다.
   ```bash
   npm run build
   ```

   - 실행 결과, `/dist` 폴더가 생성되고 정적 파일들이 이곳에 위치합니다.
   - React 프로젝트에서는 코드 난독화, 압축, 번들링, 환경 변수 주입 등이 자동으로 이루어집니다.

5. **정적 파일 배포**
   - 생성된 `build` 폴더 내의 파일들을 웹 서버(Nginx 등)나 정적 호스팅(AWS S3, Netlify 등)에 업로드해서 서비스할 수 있습니다.
   - 일부 서비스는 drag & drop만으로도 정적 파일을 배포할 수 있습니다.

## 추가 안내

- 빌드 에러 발생 시, 터미널 오류 로그를 보고 빠진 의존성 또는 환경 변수, 코드 오류 등을 점검하세요.
- `dist` 폴더는 자동 생성되는 결과물이므로, 보통 Git 등 형상관리에서는 무시(.gitignore)하는 것이 일반적입니다.
- 필요에 따라 `npm run preview` 같은 명령어를 사용하여 로컬에서도 빌드 결과물을 테스트할 수 있습니다.
  ```bash
  # dist 폴더에 빌드된 결과물을 사용해서 서버를 띄운다.
  npm run preview
  ```

## 참고

- [Vite 공식 빌드 안내](https://ko.vite.dev/guide/build.html)
- 프로젝트 구조나 빌드 방식에 따라 약간의 차이가 있을 수 있습니다.


